// reporter should be run as a stand-alone thread. It can be passed to every object to gather information.
#include "nbtp.h"
class Reporter {
public:
    void report_success(id i) {

    }

    void report_error(id i) {

    }

    void report_progress(id i) {

    }
};